services:
  mcstatus-exporter:
    build: .
    image: ghcr.io/clrxbl/blockgametracker
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      CONFIG_FILE: "/config/servers.yaml"
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./config:/config:ro
